# L2 Execution Blocks — Distributed AI-Native Orchestration Platform

## Source

- **L1 Input:** `TODO/L1-distributed-orchestration.md` (12 capabilities, CAP-01 through CAP-12)
- **Generated by:** TODO Agent (Planning Mode, L1→L2)
- **Timestamp:** 2026-03-05T00:00:00Z

---

## Phase Overview

| Phase | Name | Capabilities | Blocks | Description |
|-------|------|--------------|--------|-------------|
| 1 | Foundation | CAP-01, CAP-02, CAP-03 | 8 | Research, architecture decisions, product requirements — all knowledge artifacts that inform implementation |
| 2 | Core Infrastructure | CAP-05, CAP-04, CAP-12 | 8 | Database layer, MCP server core, Docker/CI infrastructure — the runtime foundation |
| 3 | API & Integration | CAP-06, CAP-07, CAP-08, CAP-09 | 9 | Ticket operations API, agent SDK, auth layer, webhook system — the interaction surface |
| 4 | Migration & Polish | CAP-11, CAP-10 | 5 | Dual-mode migration bridge, web dashboard — cutover and user-facing polish |

**Total: 30 L2 execution blocks**

### Block Distribution by Capability

| Capability | Blocks | IDs |
|------------|--------|-----|
| CAP-01: Research & Analysis | 3 | BLK-01-01, BLK-01-02, BLK-01-03 |
| CAP-02: Architecture Design | 4 | BLK-02-01, BLK-02-02, BLK-02-03, BLK-02-04 |
| CAP-03: Product Requirements | 1 | BLK-03-01 |
| CAP-04: MCP Server Core | 3 | BLK-04-01, BLK-04-02, BLK-04-03 |
| CAP-05: Database Layer | 3 | BLK-05-01, BLK-05-02, BLK-05-03 |
| CAP-06: Ticket API | 3 | BLK-06-01, BLK-06-02, BLK-06-03 |
| CAP-07: Agent Client Library | 2 | BLK-07-01, BLK-07-02 |
| CAP-08: Auth & Authorization | 2 | BLK-08-01, BLK-08-02 |
| CAP-09: Webhook Processing | 2 | BLK-09-01, BLK-09-02 |
| CAP-10: Dashboard | 3 | BLK-10-01, BLK-10-02, BLK-10-03 |
| CAP-11: Migration Bridge | 2 | BLK-11-01, BLK-11-02 |
| CAP-12: Infrastructure | 2 | BLK-12-01, BLK-12-02 |

---

## Phase 1 — Foundation (Research + Architecture + Requirements)

### BLK-01-01: MCP Protocol Research

| Field | Value |
|-------|-------|
| **Block ID** | BLK-01-01 |
| **Capability** | CAP-01: Research & Analysis |
| **Title** | MCP Protocol Specification Research |
| **Ticket Type** | research |
| **Dependencies** | None |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Investigate the MCP (Model Context Protocol) specification in depth. Evaluate message format, tool registration semantics, session lifecycle management, transport options (stdio vs SSE vs HTTP), error handling semantics, and SDK maturity. Produce a research report with findings, risks, and recommendations for the Architect agent.

**Acceptance Criteria (high-level):**
- MCP protocol specification fully reviewed and documented
- Transport layer options (stdio, SSE, HTTP) compared with latency/throughput data
- Session management lifecycle documented (connect, reconnect, timeout, resumption)
- Official MCP Python SDK evaluated for maturity, stability, and gaps
- Risk assessment of MCP protocol adoption produced
- Recommendation report delivered to Architect

---

### BLK-01-02: PostgreSQL Distributed Patterns Research

| Field | Value |
|-------|-------|
| **Block ID** | BLK-01-02 |
| **Capability** | CAP-01: Research & Analysis |
| **Title** | PostgreSQL Distributed Locking & Queue Patterns |
| **Ticket Type** | research |
| **Dependencies** | None |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Research PostgreSQL distributed locking mechanisms relevant to the orchestration platform. Investigate `SELECT FOR UPDATE SKIP LOCKED` for fair queue semantics, advisory locks (`pg_advisory_xact_lock`) for file-level mutex, connection pooling strategies (PgBouncer vs asyncpg application-level pooling), transaction isolation levels (READ COMMITTED vs SERIALIZABLE), and event sourcing patterns using PostgreSQL.

**Acceptance Criteria (high-level):**
- `SELECT FOR UPDATE SKIP LOCKED` patterns documented with queue semantics analysis
- Advisory lock strategies evaluated for file-path-based mutex
- Connection pooling options compared (PgBouncer vs application-level)
- Transaction isolation trade-offs analyzed for claim vs advance operations
- Event sourcing feasibility assessed for ticket history audit trail
- PoC code snippets or benchmark results included where applicable

---

### BLK-01-03: Gap Analysis & Technology Evaluation

| Field | Value |
|-------|-------|
| **Block ID** | BLK-01-03 |
| **Capability** | CAP-01: Research & Analysis |
| **Title** | Current System Gap Analysis & Technology Comparison |
| **Ticket Type** | research |
| **Dependencies** | None |
| **Estimated Effort** | S |
| **L3 Ticket Estimate** | 3–4 |

**Description:**
Perform a detailed gap analysis of the current `tickets.py` + `agent-runner.py` + `todo_visual.py` system against the distributed platform requirements. Produce a comparative analysis of MCP vs gRPC vs REST for agent communication. Evaluate web frameworks, ORMs, and migration tooling options for the backend.

**Acceptance Criteria (high-level):**
- Gap matrix mapping current capabilities to distributed requirements
- MCP vs gRPC vs REST comparison with decision recommendation
- Web framework evaluation (FastAPI, Flask, etc.) for REST API layer
- ORM/query builder evaluation (SQLAlchemy, asyncpg raw, etc.)
- Migration tool evaluation (Alembic, custom scripts)
- Summary of gaps that create migration risk

---

### BLK-02-01: System Architecture & ADRs

| Field | Value |
|-------|-------|
| **Block ID** | BLK-02-01 |
| **Capability** | CAP-02: Architecture Design |
| **Title** | System Architecture, Component Diagrams & ADRs |
| **Ticket Type** | architecture |
| **Dependencies** | BLK-01-01, BLK-01-02, BLK-01-03 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Produce the high-level system architecture for the distributed orchestration platform. Create component diagrams showing MCP Server, PostgreSQL, Git, Agent Clients, Dashboard, and Webhook Processor boundaries and interactions. Author ADRs for key decisions: PostgreSQL over alternatives, MCP as agent protocol, migration strategy, and auth model selection. Define the technology selection matrix (web framework, ORM, auth library, migration tool).

**Acceptance Criteria (high-level):**
- System component diagram with all 6 major components and their interfaces
- Data flow diagrams for claim, advance, rework, and sync operations
- ADR for PostgreSQL selection (with evaluated alternatives)
- ADR for MCP protocol adoption
- ADR for migration strategy (dual-mode, phased cutover)
- Technology selection matrix with justified choices
- Architecture review-ready deliverable for downstream agents

---

### BLK-02-02: Database Schema Design

| Field | Value |
|-------|-------|
| **Block ID** | BLK-02-02 |
| **Capability** | CAP-02: Architecture Design |
| **Title** | Database Schema Design & Data Model |
| **Ticket Type** | architecture |
| **Dependencies** | BLK-02-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Design the complete PostgreSQL schema for the platform. Define tables (`tickets`, `claims`, `lease_heartbeats`, `stage_transitions`, `event_history`, `file_locks`, `agents`, `machines`, `operators`), relationships, indexes (unique, composite, GIN on JSONB), constraints, and the event sourcing data model for audit trails. Produce an ER diagram and migration strategy document.

**Acceptance Criteria (high-level):**
- Complete ER diagram with all tables and relationships
- Index strategy defined (ticket_id unique, stage+claimed_by composite, GIN on JSONB fields)
- Event sourcing table design for complete audit trail
- Migration strategy from JSON files to PostgreSQL records
- Data integrity constraints documented
- Schema supports all operations defined in CAP-06 scope

---

### BLK-02-03: API Contract Design

| Field | Value |
|-------|-------|
| **Block ID** | BLK-02-03 |
| **Capability** | CAP-02: Architecture Design |
| **Title** | API Contracts — OpenAPI & MCP Tool Definitions |
| **Ticket Type** | architecture |
| **Dependencies** | BLK-02-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Design the API contracts for both the MCP tool interface (agent-facing) and the REST API (dashboard/external-facing). Produce OpenAPI 3.1 specifications for all REST endpoints and MCP tool schema definitions for all ticket operations. Define request/response models, error codes, pagination, and filtering semantics.

**Acceptance Criteria (high-level):**
- OpenAPI 3.1 spec covering all REST endpoints from CAP-06 scope
- MCP tool definitions with JSON Schema for all ticket operations
- Error code catalog with semantic meanings
- Pagination and filtering contract for list endpoints
- WebSocket message format specification
- Data flow diagrams for each major operation

---

### BLK-02-04: Non-Functional Requirements & Fitness Functions

| Field | Value |
|-------|-------|
| **Block ID** | BLK-02-04 |
| **Capability** | CAP-02: Architecture Design |
| **Title** | Quality Attributes, Fitness Functions & Benchmark Specs |
| **Ticket Type** | architecture |
| **Dependencies** | BLK-02-01 |
| **Estimated Effort** | S |
| **L3 Ticket Estimate** | 3–4 |

**Description:**
Define measurable quality attributes and fitness functions for the platform. Specify performance targets (p99 claim latency < 100ms), correctness invariants (concurrent claim safety), availability goals, and scalability benchmarks. Create a testing strategy document that defines how each fitness function will be verified.

**Acceptance Criteria (high-level):**
- Performance targets defined (latency, throughput, concurrent claims)
- Correctness invariants documented (claim atomicity, state transition integrity)
- Availability and recovery targets (RTO, RPO)
- Scalability benchmarks specified (max concurrent agents, max tickets)
- Fitness function verification plan (how each metric is tested)
- Zero-downtime migration fitness function defined

---

### BLK-03-01: Product Requirements Document

| Field | Value |
|-------|-------|
| **Block ID** | BLK-03-01 |
| **Capability** | CAP-03: Product Requirements |
| **Title** | PRD — User Personas, Stories & Acceptance Criteria |
| **Ticket Type** | docs |
| **Dependencies** | BLK-01-01, BLK-01-02, BLK-01-03 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Produce the Product Requirements Document for the distributed orchestration platform. Define user personas (Human Operator, AI Agent, ReaperOAK Dispatcher, System Administrator), capture user stories for each persona, specify non-functional requirements, document migration acceptance criteria, define dashboard UX requirements, and create a priority matrix mapping requirements to capabilities.

**Acceptance Criteria (high-level):**
- 4 user personas documented with goals, constraints, and interaction patterns
- User stories covering all capability domains (minimum 20 stories)
- Non-functional requirements with measurable targets
- Migration acceptance criteria (dual-mode, rollback, data integrity)
- Dashboard UX requirements (views, interactions, real-time constraints)
- Priority matrix linking requirements to L1 capabilities

---

## Phase 2 — Core Infrastructure

### BLK-05-01: Database Schema & Migrations

| Field | Value |
|-------|-------|
| **Block ID** | BLK-05-01 |
| **Capability** | CAP-05: Database Layer |
| **Title** | PostgreSQL Schema Implementation & Migration Framework |
| **Ticket Type** | backend |
| **Dependencies** | BLK-02-02, BLK-12-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Implement the PostgreSQL schema designed in BLK-02-02. Set up the migration framework (Alembic or selected tool), create initial migration scripts for all tables (`tickets`, `claims`, `lease_heartbeats`, `stage_transitions`, `event_history`, `file_locks`, `agents`, `machines`, `operators`), implement indexes, and create the database seeding script that imports existing `.github/tickets/*.json` files.

**Acceptance Criteria (high-level):**
- Migration framework initialized and configured
- Initial migration creating all tables with constraints and indexes
- GIN indexes on JSONB columns (dependencies, file_paths)
- Composite indexes on (stage, claimed_by) for claim queries
- Database seed script importing existing ticket JSON files
- Migration up/down scripts tested and reversible

---

### BLK-05-02: Distributed Locking Implementation

| Field | Value |
|-------|-------|
| **Block ID** | BLK-05-02 |
| **Capability** | CAP-05: Database Layer |
| **Title** | Distributed Locking — Claim Queue & File Mutex |
| **Ticket Type** | backend |
| **Dependencies** | BLK-05-01 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Implement the distributed locking mechanisms that replace git-push-based locking. Build the ticket claim queue using `SELECT FOR UPDATE SKIP LOCKED` for fair, non-blocking claim semantics. Implement file-level mutex using PostgreSQL advisory locks (`pg_advisory_xact_lock`) keyed on file path hashes. Build the lease heartbeat mechanism replacing the fixed 30-minute timeout with dynamic, extendable leases. Configure transaction isolation levels (READ COMMITTED for claims, SERIALIZABLE for state transitions).

**Acceptance Criteria (high-level):**
- Claim queue implemented with `SKIP LOCKED` semantics
- Advisory lock wrapper for file-path-based mutex
- Lease heartbeat mechanism with configurable intervals
- Expired lease detection and automatic release
- Transaction isolation correctly configured per operation type
- Concurrent claim safety verified under contention

---

### BLK-05-03: Connection Pooling & Event History

| Field | Value |
|-------|-------|
| **Block ID** | BLK-05-03 |
| **Capability** | CAP-05: Database Layer |
| **Title** | Connection Pool, Event Sourcing & Data Access Layer |
| **Ticket Type** | backend |
| **Dependencies** | BLK-05-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Implement the connection pooling layer (asyncpg pool or PgBouncer integration based on architecture decision). Build the event sourcing subsystem: every ticket state change produces an immutable event record in the `event_history` table, replacing the `history` array currently embedded in ticket JSON. Create the data access layer (repository pattern) that all higher-level services consume.

**Acceptance Criteria (high-level):**
- Connection pool configured with appropriate min/max connections
- Pool health monitoring and connection recycling
- Event history table capturing all state transitions with metadata
- Repository pattern for tickets, claims, and events
- Data access layer supports both sync and async operations
- Event replay capability for audit and debugging

---

### BLK-04-01: MCP Server Framework & Transport

| Field | Value |
|-------|-------|
| **Block ID** | BLK-04-01 |
| **Capability** | CAP-04: MCP Server Core |
| **Title** | MCP Server Setup, SDK Integration & Transport Layer |
| **Ticket Type** | backend |
| **Dependencies** | BLK-02-03, BLK-05-01 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Set up the MCP server using the official Python MCP SDK. Configure the transport layer for both stdio (local agents) and SSE/HTTP (remote agents). Implement the request/response lifecycle, error propagation semantics, and the fundamental server bootstrap sequence. Wire the server to the database layer from BLK-05-01 for state persistence.

**Acceptance Criteria (high-level):**
- MCP server initializes and accepts connections via stdio
- SSE/HTTP transport configured for remote agent access
- Error handling follows MCP protocol error semantics
- Server connects to PostgreSQL via the data access layer
- Request lifecycle instrumented with correlation IDs
- Server can be started and stopped cleanly

---

### BLK-04-02: Tool Registration & Session Management

| Field | Value |
|-------|-------|
| **Block ID** | BLK-04-02 |
| **Capability** | CAP-04: MCP Server Core |
| **Title** | Dynamic Tool Registry & Agent Session Management |
| **Ticket Type** | backend |
| **Dependencies** | BLK-04-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Implement the dynamic tool registration system that allows ticket operations to be registered as MCP tools with JSON Schema validation. Build agent session management: session creation, heartbeat tracking, timeout handling, and session resumption after disconnect. Each agent session maps to an identity and maintains lease state.

**Acceptance Criteria (high-level):**
- Tool registration API supporting dynamic tool addition
- JSON Schema validation for tool input parameters
- Agent session lifecycle (create, heartbeat, resume, expire)
- Session-to-identity binding for authorization
- Concurrent session handling (multiple agents simultaneously)
- Session timeout produces clean resource cleanup

---

### BLK-04-03: Server Observability & Operations

| Field | Value |
|-------|-------|
| **Block ID** | BLK-04-03 |
| **Capability** | CAP-04: MCP Server Core |
| **Title** | Structured Logging, Health Checks & Graceful Shutdown |
| **Ticket Type** | backend |
| **Dependencies** | BLK-04-01 |
| **Estimated Effort** | S |
| **L3 Ticket Estimate** | 3–4 |

**Description:**
Implement operational capabilities for the MCP server. Add structured JSON logging with correlation IDs (no PII). Build health check and readiness probe endpoints. Implement graceful shutdown with in-flight request draining. Add metrics collection points for monitoring integration.

**Acceptance Criteria (high-level):**
- Structured JSON logging with correlation IDs on every request
- No PII or secrets in log output
- Health check endpoint returning server and database status
- Readiness probe (server ready to accept requests)
- Graceful shutdown drains in-flight requests before exit
- Configurable shutdown timeout

---

### BLK-12-01: Local Development Environment

| Field | Value |
|-------|-------|
| **Block ID** | BLK-12-01 |
| **Capability** | CAP-12: Infrastructure |
| **Title** | Docker Compose, Environment Config & Dev Tooling |
| **Ticket Type** | infra |
| **Dependencies** | BLK-02-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Create the Docker Compose configuration for local development including containers for the MCP server, PostgreSQL (with persistent volume), dashboard (Node.js), and database admin tooling (pgAdmin or similar). Define environment configuration templates (`.env`), development vs production profiles, and developer tooling (`Makefile` or scripts for start, stop, migrate, seed, test).

**Acceptance Criteria (high-level):**
- Docker Compose file with all service definitions
- PostgreSQL container with persistent volume and initialization
- `.env.template` with all required variables documented
- Development and production configuration profiles
- `Makefile` or equivalent with common operations
- Single-command local development startup (`make up` or equivalent)

---

### BLK-12-02: CI/CD Pipeline & Operations

| Field | Value |
|-------|-------|
| **Block ID** | BLK-12-02 |
| **Capability** | CAP-12: Infrastructure |
| **Title** | GitHub Actions, Health Monitoring & Backup Strategy |
| **Ticket Type** | infra |
| **Dependencies** | BLK-12-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Update CI/CD pipelines for the distributed platform. Create GitHub Actions workflows for MCP server tests, database migration CI step, dashboard build/test pipeline, and integration tests (MCP client ↔ server ↔ database). Implement container health checks, database connection monitoring, PostgreSQL backup strategy (pg_dump, WAL archiving), and restore procedures.

**Acceptance Criteria (high-level):**
- GitHub Actions workflow for MCP server unit + integration tests
- Database migration step in CI (apply, validate, rollback test)
- Dashboard build and test workflow
- Container health check definitions
- PostgreSQL backup strategy documented and scripted
- Database restore procedure tested

---

## Phase 3 — API & Integration

### BLK-06-01: MCP Tool Operations

| Field | Value |
|-------|-------|
| **Block ID** | BLK-06-01 |
| **Capability** | CAP-06: Ticket API |
| **Title** | MCP Ticket Tools — Claim, Advance, Rework, Sync |
| **Ticket Type** | backend |
| **Dependencies** | BLK-04-02, BLK-05-02 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 5–8 |

**Description:**
Implement the MCP tool operations that agents consume for ticket lifecycle management. These tools replace the `tickets.py` CLI interface: `tickets.next` (claim next available by role), `tickets.claim` (claim by ID), `tickets.advance` (move to next SDLC stage), `tickets.rework` (return to implementation), `tickets.release` (release claim), `tickets.status` (query state), `tickets.sync` (dependency resolution), and `tickets.validate` (integrity check). Each tool uses the distributed locking from BLK-05-02.

**Acceptance Criteria (high-level):**
- All 8 MCP ticket tools implemented and registered
- `tickets.next` uses `SKIP LOCKED` for fair claiming
- `tickets.advance` enforces SDLC flow order per ticket type
- `tickets.rework` enforces rework count limits (max 3)
- `tickets.sync` performs dependency resolution and unblock evaluation
- Input validation against ticket schema for all operations
- Error responses follow MCP error semantics

---

### BLK-06-02: REST API Endpoints

| Field | Value |
|-------|-------|
| **Block ID** | BLK-06-02 |
| **Capability** | CAP-06: Ticket API |
| **Title** | REST API — Ticket CRUD, Filtering & History |
| **Ticket Type** | backend |
| **Dependencies** | BLK-06-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Implement REST API endpoints for the dashboard and external integrations. Build `GET /api/tickets` (list with filters), `GET /api/tickets/:id` (detail with history), `GET /api/tickets/:id/history` (event log), `POST /api/tickets/:id/claim`, `POST /api/tickets/:id/advance`, `POST /api/tickets/:id/rework`, `GET /api/stages` (pipeline overview), and `GET /api/health`. REST endpoints share the same service layer as MCP tools.

**Acceptance Criteria (high-level):**
- All REST endpoints implemented per OpenAPI contract from BLK-02-03
- Filtering by stage, type, priority, claimed_by, machine
- Pagination support on list endpoints
- Ticket detail includes full history timeline
- Shared service layer between MCP tools and REST endpoints (no duplication)
- Input validation and consistent error response format

---

### BLK-06-03: Real-time Streaming & Operational Features

| Field | Value |
|-------|-------|
| **Block ID** | BLK-06-03 |
| **Capability** | CAP-06: Ticket API |
| **Title** | WebSocket Streaming, Idempotency & Rate Limiting |
| **Ticket Type** | backend |
| **Dependencies** | BLK-06-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Implement real-time ticket state streaming via WebSocket (`/ws/tickets`), idempotency keys for claim/advance operations (preventing duplicate processing), and per-agent/machine rate limiting. The WebSocket stream broadcasts ticket state changes to connected dashboard clients.

**Acceptance Criteria (high-level):**
- WebSocket endpoint streaming real-time ticket state changes
- Clients can subscribe to filtered streams (by stage, type, etc.)
- Idempotency keys on claim and advance operations
- Rate limiting per agent identity and machine
- Graceful WebSocket disconnect handling
- Backpressure management for slow consumers

---

### BLK-07-01: Agent SDK Core

| Field | Value |
|-------|-------|
| **Block ID** | BLK-07-01 |
| **Capability** | CAP-07: Agent Client Library |
| **Title** | Python Agent SDK — Connection, Operations & Error Handling |
| **Ticket Type** | backend |
| **Dependencies** | BLK-06-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Build the core Python client library (`forgeos-agent-sdk`) that agents use to interact with the MCP server. Implement MCP client connection management (connect, reconnect, session resumption), high-level operations (`claim()`, `advance()`, `rework()`, `release()`, `get_ticket()`, `heartbeat()`), structured error handling (claim conflicts, lease expiry, network errors), and configuration via environment variables.

**Acceptance Criteria (high-level):**
- Python package with clean public API
- MCP client connection with automatic reconnection
- All high-level ticket operations implemented
- Structured exceptions for all error categories
- Configuration via environment variables (server URL, identity, transport)
- Comprehensive docstrings and usage examples

---

### BLK-07-02: SDK Extensions & Migration Shim

| Field | Value |
|-------|-------|
| **Block ID** | BLK-07-02 |
| **Capability** | CAP-07: Agent Client Library |
| **Title** | Lease Heartbeat, Summary Handoff & Filesystem Fallback |
| **Ticket Type** | backend |
| **Dependencies** | BLK-07-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Extend the agent SDK with automatic lease heartbeat (background thread/task extending lease during long operations), summary handoff helpers (read previous stage summary, write current stage summary), backward compatibility shim that falls back to filesystem operations during migration, and integration hooks for `agent-runner.py` two-commit protocol (git operations remain for code commits).

**Acceptance Criteria (high-level):**
- Background lease heartbeat extending lease automatically
- Summary handoff helpers for read/write stage summaries
- Filesystem fallback mode when MCP server is unavailable
- Feature-flag-controlled mode switching (MCP vs filesystem)
- Integration points for `agent-runner.py` git commit protocol
- Fallback mode is transparent to calling agent code

---

### BLK-08-01: Identity & Authentication

| Field | Value |
|-------|-------|
| **Block ID** | BLK-08-01 |
| **Capability** | CAP-08: Authentication & Authorization |
| **Title** | Agent, Machine & Operator Authentication |
| **Ticket Type** | backend |
| **Dependencies** | BLK-04-01, BLK-05-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Implement identity verification for all platform actors. Build agent identity via pre-shared API keys or JWT tokens per agent role. Implement machine registration with `machine_id` verification. Add operator authentication (token-based or OAuth). Store identities in the database. Enforce secret management best practices (environment variables, no hardcoded credentials).

**Acceptance Criteria (high-level):**
- Agent authentication via API key or JWT per role
- Machine registration and identity verification
- Operator authentication with token management
- Identity storage in PostgreSQL (agents, machines, operators tables)
- Secret management via environment variables only
- Authentication middleware for both MCP and REST layers

---

### BLK-08-02: Authorization & Audit

| Field | Value |
|-------|-------|
| **Block ID** | BLK-08-02 |
| **Capability** | CAP-08: Authentication & Authorization |
| **Title** | Role-Based Permissions, Stage Guards & Audit Logging |
| **Ticket Type** | backend |
| **Dependencies** | BLK-08-01, BLK-06-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Implement authorization rules: agents can only claim tickets matching their SDLC stage (Backend→BACKEND, QA→QA, etc.), operators can claim on behalf of any agent role but only on their registered machines, admin role grants force-release/force-advance/system-config capabilities. Add comprehensive audit logging of all authenticated operations. Implement per-identity rate limiting.

**Acceptance Criteria (high-level):**
- Role-based claim restrictions enforced (agent ↔ stage mapping)
- Operator permissions scoped to registered machines
- Admin role with elevated capabilities (force operations)
- Audit log capturing identity, operation, target, timestamp, result
- Rate limiting per identity (agent, operator, machine)
- Security review ticket generated for threat model validation

---

### BLK-09-01: Inbound Webhook Processing

| Field | Value |
|-------|-------|
| **Block ID** | BLK-09-01 |
| **Capability** | CAP-09: Webhook Processing |
| **Title** | Webhook Receiver — GitHub Events & Signature Verification |
| **Ticket Type** | backend |
| **Dependencies** | BLK-06-01, BLK-08-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Build the inbound webhook HTTP endpoint that accepts JSON payloads from external systems. Implement GitHub webhook integration: push events trigger `tickets.sync`, CI status events update ticket stage on pass/fail, PR events link pull requests to ticket IDs. Add webhook signature verification (HMAC for GitHub, configurable for custom sources). Route events to appropriate internal handlers.

**Acceptance Criteria (high-level):**
- HTTP endpoint accepting webhook payloads
- GitHub webhook signature verification (HMAC-SHA256)
- Push event handler triggering dependency sync
- CI status event handler updating ticket stage
- PR event handler linking PRs to ticket IDs
- Webhook payload validation and error handling

---

### BLK-09-02: Outbound Notification System

| Field | Value |
|-------|-------|
| **Block ID** | BLK-09-02 |
| **Capability** | CAP-09: Webhook Processing |
| **Title** | Notification Engine — State Changes, Alerts & Delivery |
| **Ticket Type** | backend |
| **Dependencies** | BLK-09-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–4 |

**Description:**
Build the outbound notification system. Emit notifications on ticket state changes, claim expiry warnings, and rework events with rejection reasons. Support configurable notification channels (outbound webhook URLs, Slack incoming webhooks). Implement an event queue (in-database) for reliable delivery with retry logic and exponential backoff for failed deliveries.

**Acceptance Criteria (high-level):**
- Event queue for reliable notification delivery
- Notifications emitted on state change, claim expiry, rework
- Configurable notification channels (webhook URL, Slack)
- Retry logic with exponential backoff (max retries configurable)
- Dead-letter handling for permanently failed deliveries
- Notification payload includes ticket context and change details

---

## Phase 4 — Migration & Polish

### BLK-10-01: Dashboard UI Design

| Field | Value |
|-------|-------|
| **Block ID** | BLK-10-01 |
| **Capability** | CAP-10: Dashboard |
| **Title** | Dashboard UI/UX Design & Component Mockups |
| **Ticket Type** | frontend |
| **Dependencies** | BLK-03-01 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 3–5 |

**Description:**
Design the web dashboard UI/UX. Create wireframes and mockups for all views: stage pipeline (Kanban board), ticket detail, dependency graph (interactive DAG), active claims monitor, operator workbench, multi-machine status, and system health. Define the component library, design tokens (colors, spacing, typography), dark/light theme specifications, and responsive layout breakpoints. The UIDesigner agent produces these artifacts before Frontend implementation begins.

**Acceptance Criteria (high-level):**
- Wireframes for all 7 dashboard views
- Component library specification (buttons, cards, modals, tables)
- Design tokens defined (dark + light theme)
- Responsive layout breakpoints (desktop-first, tablet-compatible)
- Interaction patterns for operator actions (claim, release, advance)
- Mockup approval status documented per the UI Gate requirement

---

### BLK-10-02: Dashboard Core Views

| Field | Value |
|-------|-------|
| **Block ID** | BLK-10-02 |
| **Capability** | CAP-10: Dashboard |
| **Title** | Dashboard Implementation — Pipeline, Ticket Detail & Dependency Graph |
| **Ticket Type** | frontend |
| **Dependencies** | BLK-10-01, BLK-06-02 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 5–7 |

**Description:**
Implement the core dashboard views as a web application (React/Next.js or similar SPA). Build the stage pipeline view (Kanban-style board with ticket counts per stage), ticket detail view (full metadata, history timeline, dependency tree), and interactive dependency graph (DAG visualization replacing `--dot` output from `todo_visual.py`). Connect to the REST API from BLK-06-02 and WebSocket stream from BLK-06-03 for real-time updates.

**Acceptance Criteria (high-level):**
- Web application scaffolded with chosen framework
- Stage pipeline view with drag-free Kanban display and ticket counts
- Ticket detail view with metadata, history timeline, and dependency tree
- Interactive dependency graph with zoom/pan
- Real-time updates via WebSocket connection
- Search by ticket ID, title, and file paths
- Dark/light theme support

---

### BLK-10-03: Dashboard Operations & Advanced Features

| Field | Value |
|-------|-------|
| **Block ID** | BLK-10-03 |
| **Capability** | CAP-10: Dashboard |
| **Title** | Operator Workbench, Claim Monitor & Multi-Machine View |
| **Ticket Type** | frontend |
| **Dependencies** | BLK-10-02, BLK-08-02 |
| **Estimated Effort** | M |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Implement advanced dashboard features: active claims monitor (agent, machine, operator, lease countdown timer), operator workbench (claim/release/advance actions from UI with auth), multi-machine status view (which machines are active, which agents are running), and system health dashboard (database connections, MCP server status, webhook delivery rate). Integrate with authentication from BLK-08-02 for operator action authorization.

**Acceptance Criteria (high-level):**
- Active claims monitor with real-time lease countdown
- Operator workbench with authenticated claim/release/advance actions
- Multi-machine status showing active machines and running agents
- System health dashboard with database and MCP server status
- Filtering by stage, type, priority, operator, machine, agent
- All operator actions require authentication

---

### BLK-11-01: Dual-Mode Engine & Data Synchronization

| Field | Value |
|-------|-------|
| **Block ID** | BLK-11-01 |
| **Capability** | CAP-11: Migration Bridge |
| **Title** | Dual-Mode Wrapper, Feature Flags & Bidirectional Sync |
| **Ticket Type** | backend |
| **Dependencies** | BLK-05-02, BLK-06-01, BLK-07-01 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 5–7 |

**Description:**
Build the dual-mode engine that allows the system to operate on both filesystem and MCP/database simultaneously during migration. Create a `tickets.py` wrapper that reads from PostgreSQL (primary) with filesystem fallback, writes to both during transition, and supports feature-flag-controlled mode switching per operation. Implement bidirectional data synchronization: import existing `.github/tickets/*.json` and `.github/ticket-state/` into PostgreSQL, periodic sync between filesystem and database, and conflict resolution (database wins, filesystem advisory).

**Acceptance Criteria (high-level):**
- Dual-mode wrapper reading from DB with filesystem fallback
- Feature flags controlling mode per operation type
- Data import from filesystem to PostgreSQL (full initial sync)
- Periodic bidirectional sync during dual-mode period
- Conflict resolution strategy (database authoritative)
- Database → filesystem export for rollback capability

---

### BLK-11-02: Agent Migration Path & Validation

| Field | Value |
|-------|-------|
| **Block ID** | BLK-11-02 |
| **Capability** | CAP-11: Migration Bridge |
| **Title** | Phased Agent Migration, Shadow Mode & Rollback |
| **Ticket Type** | backend |
| **Dependencies** | BLK-11-01, BLK-07-02 |
| **Estimated Effort** | L |
| **L3 Ticket Estimate** | 4–6 |

**Description:**
Implement the phased agent migration path: Phase A (agents use filesystem, background sync to DB), Phase B (agents use SDK with filesystem fallback), Phase C (agents use SDK exclusively, filesystem read-only), Phase D (filesystem deprecated). Build shadow mode validation: run both paths in parallel, compare results, log divergences. Implement automated rollback triggers on MCP server health check failure. Evolve `agent-runner.py`: Phase 1 unchanged, Phase 2 CLAIM via MCP + WORK via git, Phase 3 full MCP claim/advance with git work commits.

**Acceptance Criteria (high-level):**
- Migration phases A through D implemented with clear transition gates
- Shadow mode comparing filesystem vs database results
- Divergence logging with structured reports
- Automated rollback triggering on health check failure
- `agent-runner.py` evolution across migration phases
- Regression test suite validating each migration phase transition

---

## Inter-Block Dependency Graph

```
Phase 1 — Foundation
========================

BLK-01-01 (MCP Research)          ──┐
BLK-01-02 (PG Patterns Research)  ──┼──→ BLK-02-01 (System Arch & ADRs)
BLK-01-03 (Gap Analysis)          ──┤        │
                                    │        ├──→ BLK-02-02 (DB Schema Design)
                                    │        ├──→ BLK-02-03 (API Contract Design)
                                    │        └──→ BLK-02-04 (NFR & Fitness Functions)
                                    │
                                    └──→ BLK-03-01 (PRD)


Phase 2 — Core Infrastructure
========================

BLK-02-01 ──→ BLK-12-01 (Dev Environment) ──→ BLK-12-02 (CI/CD & Ops)

BLK-02-02 ──┐
BLK-12-01 ──┴──→ BLK-05-01 (DB Schema & Migrations) ──┬──→ BLK-05-02 (Distributed Locking)
                                                        └──→ BLK-05-03 (Conn Pool & Events)

BLK-02-03 ──┐
BLK-05-01 ──┴──→ BLK-04-01 (MCP Server Framework) ──┬──→ BLK-04-02 (Tool Reg & Sessions)
                                                      └──→ BLK-04-03 (Server Observability)


Phase 3 — API & Integration
========================

BLK-04-02 ──┐
BLK-05-02 ──┴──→ BLK-06-01 (MCP Tool Ops) ──┬──→ BLK-06-02 (REST API)
                                              ├──→ BLK-06-03 (WebSocket & Rate Limiting)
                                              ├──→ BLK-07-01 (Agent SDK Core)
                                              │        └──→ BLK-07-02 (SDK Extensions)
                                              └──→ BLK-08-02 (Authorization & Audit) ←── BLK-08-01

BLK-04-01 ──┐
BLK-05-01 ──┴──→ BLK-08-01 (Identity & Auth)

BLK-06-01 ──┐
BLK-08-01 ──┴──→ BLK-09-01 (Inbound Webhooks) ──→ BLK-09-02 (Outbound Notifications)


Phase 4 — Migration & Polish
========================

BLK-03-01 ──→ BLK-10-01 (UI Design) ──→ BLK-10-02 (Core Views) ──→ BLK-10-03 (Advanced Features)
BLK-06-02 ──────────────────────────────→ BLK-10-02                       ↑
BLK-08-02 ────────────────────────────────────────────────────────────────┘

BLK-05-02 ──┐
BLK-06-01 ──┼──→ BLK-11-01 (Dual-Mode Engine) ──→ BLK-11-02 (Agent Migration)
BLK-07-01 ──┘                                          ↑
BLK-07-02 ──────────────────────────────────────────────┘
```

---

## Critical Path

The longest dependency chain through all phases determines the minimum timeline:

```
BLK-01-01/02/03  →  BLK-02-01  →  BLK-02-02  →  BLK-05-01  →  BLK-05-02  →  BLK-06-01  →  BLK-07-01  →  BLK-07-02  →  BLK-11-01  →  BLK-11-02
  Research          Sys Arch      DB Design     DB Schema     Dist Lock      MCP Tools     SDK Core      SDK Ext       Dual-Mode     Migration
```

**Critical path length: 10 blocks**

**Secondary critical path (Dashboard):**
```
BLK-01-01/02/03  →  BLK-03-01  →  BLK-10-01  →  BLK-10-02  →  BLK-10-03
  Research           PRD           UI Design     Core Views    Adv Features
```

This secondary path is shorter (5 blocks) and merges at BLK-10-02 which also needs BLK-06-02. The REST API chain (BLK-06-01 → BLK-06-02) is the gating factor for dashboard implementation, not the UI design.

---

## Parallelizable Block Groups

### Phase 1 Parallelization

| Parallel Group | Blocks | Rationale |
|---------------|--------|-----------|
| P1-A (Research) | BLK-01-01, BLK-01-02, BLK-01-03 | All 3 research blocks have zero dependencies — fully parallel |
| P1-B (Architecture) | BLK-02-02, BLK-02-03, BLK-02-04 | All depend only on BLK-02-01 — parallel after system arch completes |
| P1-C (Cross-cut) | BLK-03-01 | Can start in parallel with architecture after research completes |

**Phase 1 minimum sequential steps:** 3 (Research → System Arch → Schema/API/NFR in parallel)

### Phase 2 Parallelization

| Parallel Group | Blocks | Rationale |
|---------------|--------|-----------|
| P2-A (DB + Infra) | BLK-05-01, BLK-12-01 | BLK-05-01 needs BLK-02-02 + BLK-12-01; BLK-12-01 needs only BLK-02-01 — BLK-12-01 can start first |
| P2-B (DB internals) | BLK-05-02, BLK-05-03 | Both depend only on BLK-05-01 — parallel after schema |
| P2-C (MCP branches) | BLK-04-02, BLK-04-03 | Both depend only on BLK-04-01 — parallel after server framework |
| P2-D (CI) | BLK-12-02 | Depends on BLK-12-01 — runs alongside DB/MCP work |

**Phase 2 minimum sequential steps:** 4 (Infra → Schema → Locking + Server Framework → Tool Reg + Sessions)

### Phase 3 Parallelization

| Parallel Group | Blocks | Rationale |
|---------------|--------|-----------|
| P3-A (Post-MCP-Tools) | BLK-06-02, BLK-06-03, BLK-07-01 | All 3 depend on BLK-06-01 — parallel after MCP tools |
| P3-B (Auth) | BLK-08-01 | Depends on BLK-04-01 + BLK-05-01 — can start early in Phase 3 |
| P3-C (Post-Auth) | BLK-08-02, BLK-09-01 | Both need BLK-08-01 + BLK-06-01 — parallel after auth + MCP tools |
| P3-D (Post-Webhook) | BLK-09-02 | Depends on BLK-09-01 |
| P3-E (SDK Ext) | BLK-07-02 | Depends on BLK-07-01 |

**Phase 3 minimum sequential steps:** 3 (MCP Tools → REST/WS/SDK/Auth in parallel → Auth, Webhooks, SDK extensions)

### Phase 4 Parallelization

| Parallel Group | Blocks | Rationale |
|---------------|--------|-----------|
| P4-A (Parallel starts) | BLK-10-01, BLK-11-01 | Independent — UI design vs migration engine |
| P4-B (Dashboard build) | BLK-10-02 | Needs BLK-10-01 + BLK-06-02 |
| P4-C (Final parallel) | BLK-10-03, BLK-11-02 | Independent — dashboard polish vs agent migration |

**Note:** BLK-10-01 (UI Design) can begin as soon as BLK-03-01 (PRD) completes in Phase 1, potentially overlapping with Phase 2/3 work. This is a significant parallelization opportunity.

**Phase 4 minimum sequential steps:** 3 (Dual-mode + UI Design → Core views + Agent migration → Advanced features)

---

## Effort Summary

| Effort | Count | Blocks |
|--------|-------|--------|
| XS | 0 | — |
| S | 3 | BLK-01-03, BLK-02-04, BLK-04-03 |
| M | 18 | BLK-01-01, BLK-01-02, BLK-02-02, BLK-02-03, BLK-03-01, BLK-05-01, BLK-05-03, BLK-04-02, BLK-12-01, BLK-12-02, BLK-06-02, BLK-06-03, BLK-07-01, BLK-07-02, BLK-08-01, BLK-08-02, BLK-09-01, BLK-09-02, BLK-10-01, BLK-10-03 |
| L | 9 | BLK-02-01, BLK-05-02, BLK-04-01, BLK-06-01, BLK-10-02, BLK-11-01, BLK-11-02 |
| XL | 0 | — |

---

## Ticket Type Distribution

| Ticket Type | Count | Blocks |
|-------------|-------|--------|
| research | 3 | BLK-01-01, BLK-01-02, BLK-01-03 |
| architecture | 4 | BLK-02-01, BLK-02-02, BLK-02-03, BLK-02-04 |
| docs | 1 | BLK-03-01 |
| backend | 15 | BLK-05-01, BLK-05-02, BLK-05-03, BLK-04-01, BLK-04-02, BLK-04-03, BLK-06-01, BLK-06-02, BLK-06-03, BLK-07-01, BLK-07-02, BLK-08-01, BLK-08-02, BLK-09-01, BLK-09-02 |
| infra | 2 | BLK-12-01, BLK-12-02 |
| frontend | 3 | BLK-10-01, BLK-10-02, BLK-10-03 |
| backend (migration) | 2 | BLK-11-01, BLK-11-02 |

---

## Traceability Matrix

| L1 Capability | L2 Blocks | Expected L3 Tickets |
|---------------|-----------|--------------------:|
| CAP-01: Research & Analysis | BLK-01-01, BLK-01-02, BLK-01-03 | 9–14 |
| CAP-02: Architecture Design | BLK-02-01, BLK-02-02, BLK-02-03, BLK-02-04 | 13–20 |
| CAP-03: Product Requirements | BLK-03-01 | 3–5 |
| CAP-04: MCP Server Core | BLK-04-01, BLK-04-02, BLK-04-03 | 10–15 |
| CAP-05: Database Layer | BLK-05-01, BLK-05-02, BLK-05-03 | 11–17 |
| CAP-06: Ticket API | BLK-06-01, BLK-06-02, BLK-06-03 | 12–19 |
| CAP-07: Agent Client Library | BLK-07-01, BLK-07-02 | 6–10 |
| CAP-08: Auth & Authorization | BLK-08-01, BLK-08-02 | 6–10 |
| CAP-09: Webhook Processing | BLK-09-01, BLK-09-02 | 6–9 |
| CAP-10: Dashboard | BLK-10-01, BLK-10-02, BLK-10-03 | 12–18 |
| CAP-11: Migration Bridge | BLK-11-01, BLK-11-02 | 9–13 |
| CAP-12: Infrastructure | BLK-12-01, BLK-12-02 | 7–11 |
| **TOTAL** | **30 blocks** | **104–161 tickets** |

---

## Files Read for Context Derivation

1. `.github/guardian/STOP_ALL` — halt gate (CLEAR)
2. `.github/instructions/core.instructions.md` — system identity, boot sequence, memory gate
3. `.github/instructions/sdlc.instructions.md` — stage lifecycle, post-chain, rework rules
4. `.github/instructions/ticket-system.instructions.md` — state machine, tickets.py contract
5. `.github/instructions/git-protocol.instructions.md` — two-commit protocol, scoped git
6. `.github/instructions/agent-behavior.instructions.md` — worker model, scope enforcement
7. `.github/vibecoding/chunks/TODO.agent/chunk-01.yaml` — decomposition protocol, task format
8. `.github/vibecoding/chunks/TODO.agent/chunk-02.yaml` — governance, lifecycle, completion
9. `.github/vibecoding/catalog.yml` — semantic chunk catalog
10. `TODO/L1-distributed-orchestration.md` — L1 capability breakdown (primary input)

---

*Generated by TODO Agent (Planning Mode, L1→L2) — 2026-03-05T00:00:00Z*
